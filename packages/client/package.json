{
  "name": "@patchwork/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@patchwork/content": "workspace:*",
    "@patchwork/protocol": "workspace:*",
    "pixi.js": "^8.10.2"
  },
  "devDependencies": {
    "vite": "^7.0.2"
  }
}
