# Generated Sprite Atlas

`spritesheet.png` and `spritesheet.json` are produced by:

```bash
pnpm sprites:atlas
```

The files are optional during development. If they are absent, the Pixi client uses procedural rendering.
