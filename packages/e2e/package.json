{
  "name": "@patchwork/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "run": "tsx src/run.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@patchwork/server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "playwright": "^1.61.1",
    "tsx": "^4.20.3",
    "vitest": "^3.2.4"
  }
}
